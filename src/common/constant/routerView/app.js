/**
 * @name AppPath
 * @memberof common/constant/app
 * @type {Object}
 * @return {Object} List of items object with label, to, exact and icon properties
 */
const main = '/';
export default {
  default: `${main}`,
  paragraph: {
    info: '/paragraph',
  },
};
